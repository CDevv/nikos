# nikOS
Just a side hobby project about OSDevelopment

# Building it?
You can build it on Linux or on WSL

Install the required packages
```
sudo apt-get install g++ binutils libc6-dev-i386 grub-common grub-pc-bin
```

"Make" the iso using the `all` rule in the Makefile
```
make
```